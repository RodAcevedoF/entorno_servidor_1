declare module 'generated/prisma' {
  // Minimal declaration for the generated Prisma client used in this repo
  export class PrismaClient {
    constructor();
    $disconnect(): Promise<void>;
  }
  export default PrismaClient;
}
