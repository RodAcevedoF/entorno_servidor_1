<h2>Available Sessions</h2>

<p class="intro-sessions">
	Explore our group sessions for connecting with higher consciousness.
</p>

<div class="sessions-grid">
	<% sessions.forEach(function(s) { %>
	<div class="session-card">
		<h3><%= s.title %></h3>
		<p class="description"><%= s.description %></p>
		<div class="details">
			<span class="date"
				>📅 <%= formatDate(s.date) %></span
			>
			<span class="time">🕐 <%= formatTime(s.time) %>h</span>
			<span class="spots">👥 <%= s.places %> spots</span>
		</div>
		<div class="price"><%= s.price %>€</div>
		<% if (user) { %>
		<form action="/cart/add" method="POST">
			<input type="hidden" name="sessionId" value="<%= s.id %>" />
			<button type="submit" class="button">Add to Cart</button>
		</form>
		<% } else { %>
		<p class="login-note"><a href="/login">Log in</a> to book</p>
		<% } %>
	</div>
	<% }); %>
</div>
