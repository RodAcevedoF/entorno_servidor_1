<h2>Preferences</h2>

<section class="preferences-section">
	<h3>Theme</h3>
	<p>Select your preferred display mode:</p>

	<form action="/preferences" method="POST" class="form-theme">
		<div class="theme-options">
			<label class="theme-option <%= theme === 'light' ? 'active' : '' %>">
				<input type="radio" name="theme" value="light" <%= theme === 'light' ?
				'checked' : '' %>>
				<span class="theme-preview theme-light"></span>
				<span>Light Mode</span>
			</label>

			<label class="theme-option <%= theme === 'dark' ? 'active' : '' %>">
				<input type="radio" name="theme" value="dark" <%= theme === 'dark' ?
				'checked' : '' %>>
				<span class="theme-preview theme-dark"></span>
				<span>Dark Mode</span>
			</label>
		</div>

		<button type="submit">Save Preferences</button>
	</form>
</section>

<p class="note">
	Your preference will be saved in a cookie and will be preserved even if you close the
	browser.
</p>
